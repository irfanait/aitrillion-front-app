# Node dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Testing
/coverage

# Next.js build output
/.next/
# Optional: If you don't want to track static exports
/out/

# Production build directory (optional)
# Remove /out/ if you need to version exported static files
/build

# OS / Editor metadata
.DS_Store
*.pem

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# **/package-lock.json

# Local environment variables
.env
.env.*
!.env.example

# Deployment
.vercel

# TypeScript (if added later)
/*.tsbuildinfo
next-env.d.ts

# Mac system files
Icon?

# Logs
*.log
logs
*.log.*

# Optional: VSCode config (if not shared across team)
/.vscode

# Claude Code documentation (local only)
CLAUDE.md
